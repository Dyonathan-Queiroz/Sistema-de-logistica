# =============================================================
#  CONFIGURAÇÃO DO AMBIENTE — Sistema Logístico
# =============================================================
#
#  Como usar:
#    1. Copie este arquivo:  cp .env.example .env
#    2. Preencha os valores reais no .env
#    3. NUNCA comite o arquivo .env no git (já está no .gitignore)
#
# =============================================================

# ── Aplicação ────────────────────────────────────────────────────────────────
DATABASE_URL=mysql+pymysql://root:SENHA@localhost:3306/sistema_logistico
SECRET_KEY=gere_com__python_-c__import_secrets_print_secrets.token_hex_32
COOKIE_SECURE=false

# ── Docker — MySQL ────────────────────────────────────────────────────────────
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=sistema_logistico

# ── Docker — Sync Agent (Oracle/Consinco) ─────────────────────────────────────
ORACLE_HOST=192.168.16.181
ORACLE_PORT=1521
ORACLE_SERVICE=WINT
ORACLE_USER=
ORACLE_PASSWORD=
API_USERNAME=sync_consinco
API_PASSWORD=
